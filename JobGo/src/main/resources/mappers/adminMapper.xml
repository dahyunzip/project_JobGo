<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.AdminMapper">

	<!-- 일반회원 목록 -->
	<select id="getAllNormalMembers" resultType="com.itwillbs.domain.MemberVO">
		SELECT id, userid, name, email, phone, membertype
		FROM member
		WHERE membertype = 'M' AND is_deleted = 0
		ORDER BY id DESC
	</select>

	<!-- 기업회원 목록 -->
	<select id="getAllCorpMembers" resultType="com.itwillbs.domain.MemberVO">
		SELECT id, userid, name, email, phone, membertype
		FROM member
		WHERE membertype = 'C' AND is_deleted = 0
		ORDER BY id DESC
	</select>

	<!-- 회원 삭제 -->
	<delete id="deleteMember" parameterType="int">
		DELETE FROM member WHERE id = #{id}
	</delete>

</mapper>